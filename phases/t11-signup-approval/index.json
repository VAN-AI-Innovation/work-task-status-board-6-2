{
  "project": "work-task-status-board",
  "phase": "t11-signup-approval",
  "steps": [
    {
      "step": 0,
      "name": "auth-schema",
      "commit": "chore: 회원가입·승인 마이그레이션 SQL 작성",
      "status": "completed",
      "summary": "supabase/migrations/0005_signup_approval.sql 작성·원격 적용 완료(t11_signup_approval). profiles에 status(pending|active|rejected, default active)·display_name 추가. my_role()·my_team()·my_member_id() 셋 모두 status='active' 게이트 — my_role()만 고치면 goal_metrics_select_scope의 둘째 갈래가 my_role()을 안 봐서 대기 계정에 목표 지표가 샌다. 트리거 on_auth_user_created→handle_new_user()가 role='member'·status='pending'을 하드코딩하고 team_id는 teams에 실재할 때만 넣는다(user_metadata는 사용자가 고칠 수 있는 자리). 함수 시그니처: pending_requests() / member_directory() / can_review_join(target uuid) / approve_join(target uuid, member_id uuid default null, new_member_name text default null) / reject_join(target uuid) / request_join(team text) / set_role(target uuid, new_role text, new_team text default null). set_role은 'admin'을 받지 않고, request_join은 대상이 언제나 auth.uid()다. 실행 권한: authenticated에 위 6개만(can_review_join·handle_new_user 제외). ⚠ Supabase는 public 스키마에 alter default privileges로 authenticated에 직접 grant를 걸므로 revoke는 반드시 'from public, anon, authenticated'여야 한다 — from public만으로는 안 걷힌다(실측). 새 RLS 정책 0개, insert/update/delete GRANT 0개. 0003의 정책 12개 그대로."
    },
    {
      "step": 1,
      "name": "session-status",
      "commit": "feat: 대기·거절 상태를 세션 해석에 추가",
      "status": "completed",
      "started_at": "2026-08-27T20:18:18+0900",
      "summary": "SessionOutcome 유니온이 다섯이 됐다 — {status:'anonymous'} | {status:'no_profile', userId, email} | {status:'pending'|'rejected', userId, email, teamId: TeamKey|null, displayName: string|null} | {status:'ok', viewer: Viewer}. pending·rejected는 WaitingOutcome 하나를 공유하므로 네 필드가 글자 그대로 같다. resolveSession의 profiles select가 'role, team_id, status, display_name' 한 번으로 늘었다(왕복 추가 없음). 판정 순서: 알 수 없는 role → no_profile(상태보다 먼저) / status==='rejected' → rejected / status!=='active'인 나머지 전부(undefined·null·'suspended' 포함) → pending / 'active' → ok. pending·rejected는 members를 읽지 않는다(tables === ['profiles']). toAccount는 no_profile·pending·rejected를 한 갈래로 묶어 {email, role:null}을 돌려준다 — default 절 없이 switch가 유니온을 전수한다. viewer-role.ts는 한 줄도 고치지 않았다(첫 줄 status==='ok'가 대기·거절을 다음 규칙으로 흘려보낸다). 테스트로 고정: 프로덕션+live에서 pending·rejected는 member이고 ?as=admin이 못 바꾼다. 가짜 클라이언트 둘(viewer-session.test.ts·viewer-storage.test.ts)의 profiles 행에 status:'active'를 채웠다 — DB 컬럼이 not null default 'active'라 실제 행에는 언제나 값이 있다. 게이트: lint·build 통과, npm test 1681건 전부 통과(94파일). grep -c getSession → 0.",
      "completed_at": "2026-08-27T20:24:11+0900"
    },
    {
      "step": 2,
      "name": "route-guard-signup",
      "commit": "feat: 가입·대기 경로를 라우트 가드에 반영",
      "status": "completed",
      "started_at": "2026-08-27T20:25:06+0900",
      "completed_at": "2026-08-27T20:36:34+0900",
      "summary": "가드 계층 둘로 갈랐다. (1) route-guard.ts의 PUBLIC_PREFIXES에 '/signup'만 추가 — /api/auth/signup은 기존 '/api/auth' 접두사에 이미 걸려 더 넓히지 않았고, 접두사 비교는 pathname === prefix || startsWith(prefix + '/') 그대로라 /signups는 page다. **/pending은 공개 목록에 넣지 않았다**(AC grep 통과) — 대기 사용자는 로그인한 상태라 공개로 두면 로그아웃 상태에서도 열리고 proxy가 세션 갱신을 건너뛴다. proxy.ts는 한 줄도 고치지 않았다(grep 'from(' → 0). (2) 새 순수 함수 src/lib/auth/pending-gate.ts: gateForSession(outcome: SessionOutcome, pathname: string): GateDecision, GateDecision = {kind:'allow'} | {kind:'redirect', to: string} | {kind:'deny'}, 상수 PENDING_PATH='/pending'. 판정 순서: ok·anonymous → allow / ESCAPE_PREFIXES (['/pending','/api/auth']) 정확일치·하위 → allow / '/api' 이하 → deny / 그 밖 → redirect. pending·rejected·no_profile 셋을 한 갈래로 태운다(트리거 실패로 profiles 행이 없는 계정도 /pending이 할 말을 갖는다). 새 에러 코드 **PENDING_APPROVAL(403)** — 문구 '아직 승인되지 않은 계정입니다. 승인 후에 이용할 수 있습니다.' 401을 쓰지 않는 이유는 이미 로그인했다는 것. api-error.ts 머리말이 '코드는 문서를 먼저 고친다'로 못박고 있어 docs/ARCHITECTURE.md의 X1 목록에도 추가했다(테스트가 그 목록을 손으로 옮겨 적어 대조한다 — 이제 18개). 호출부: 화면 5곳(/, /teams/[slug], /upload, /extract, /report)이 currentViewerContext() 직후 gateForSession → redirect, API 6파일 7곳(tasks GET, tasks/[id] GET·PATCH, stats, alerts, goals, report/weekly)이 gateForSession → errorResponse('PENDING_APPROVAL'). **PATCH에서는 기존 401 판정보다 앞에 둔다** — 세 상태가 status!=='ok'라 아래 줄에 그대로 걸려 401이 나가므로 순서가 곧 문구다. request-viewer.ts에 래퍼를 두지 않았다: page.test.ts 3곳이 그 모듈을 팩토리로 통째 mock해서 export를 늘리면 무관한 테스트 스캐폴딩을 고쳐야 하고, 번역은 2줄짜리 균일한 코드다(규칙 자체는 한 곳에 있다). /pending 자기 자신 allow를 pending-gate.test.ts 맨 앞에 못박았다(리다이렉트 루프가 유일한 치명 경로). 행동 변경 둘: no_profile 계정이 이제 대시보드 대신 /pending으로 가고(page.test.ts의 해당 단언을 redirect digest 대조로 바꿨다), PATCH의 no_profile이 401 → 403이 됐다. src/app/pending/page.tsx는 **자리표시자**(step 4가 문구·재요청·거절 갈래를 만든다) — 없으면 사용자가 404를 본다. app-sidebar의 HIDDEN_ON에 '/pending'을 더했다(링크가 전부 제자리로 돌아온다). 게이트: lint·build 통과, npm test 1712건 전부 통과(95파일, +31)."
    },
    {
      "step": 3,
      "name": "signup-flow",
      "commit": "feat: 회원가입 라우트와 화면 구현",
      "status": "completed",
      "started_at": "2026-08-27T21:02:41+0900",
      "completed_at": "2026-08-27T21:14:11+0900",
      "summary": "만든 파일 넷 — src/lib/api/signup-schema.ts(+test) · src/lib/auth/pwned-password.ts(+test) · src/app/api/auth/signup/route.ts(+test) · src/app/signup/page.tsx + src/components/auth/signup-form.tsx. 비밀번호 최소 길이 상수는 **MIN_PASSWORD_LENGTH = 10**(signup-schema.ts)이고 화면(minLength·안내 문구)과 서버가 같은 상수를 본다. signupSchema = {displayName trim 1~40, email(zod v4 z.email()), password min(MIN_PASSWORD_LENGTH), teamId z.enum(TEAM_IDS)} — TEAM_IDS는 `as const satisfies readonly TeamKey[]`라 팀 키가 바뀌면 컴파일에서 먼저 빨개진다. **AC grep `password` 때문에 라우트가 자격증명을 직접 만지지 않는다** — signup-schema.ts가 셋을 다 진다: readSignup(폼만, 던지지 않고 null) · isBreachedSignup(signup, deps)(pwned-password로 위임) · toSignUpCredentials(signup)(Auth payload). toSignUpCredentials의 options.data는 {display_name, team_id} **둘뿐**이고 role·status를 싣지 않는다(트리거가 하드코딩 — 실어 보내면 다음 사람이 트리거도 읽는다고 착각한다). pwned-password.ts는 k-익명성: SHA-1 대문자 40자 중 **앞 5글자만** api.pwnedpasswords.com/range/{prefix}로 나가고 `Add-Padding: true`를 붙이며, **패딩은 count 0으로 오므로 건수를 반드시 본다**(안 보면 패딩이 곧 오탐이다). fetch를 인자로 받고 timeoutMs 기본 2000(AbortController). 타임아웃·네트워크·4xx/5xx·본문 읽기 실패가 전부 false = **fail-open**이며 근거를 파일 머리말에 적었다(외부 서비스 장애가 회원가입 장애가 되면 안 된다 — 문은 최소 길이와 승인 절차가 진다). 라우트 갈래: 본문 실패→invalid / 자격증명 없음→unavailable / 유출→weak(계정을 만들지 않는다) / signUp 실패·예외→**사유 불문 invalid** (이미 가입된 이메일도 같은 답 — 계정 존재 확인 도구가 되지 않게) / 성공+세션→303 /pending + 쿠키 / 성공+세션 없음→303 /signup?sent=1 (Confirm email 설정을 코드가 알 필요 없게 세션 유무로만 가른다). 유출 검사는 createSessionClient **뒤**에 둔다 — 붙을 Auth 서버가 없는 환경에서 바깥 호출을 한 번 아낀다(여전히 signUp 전이다). 화면: 필드 넷(이름·이메일·비밀번호·팀 select), 팀 라벨은 TEAM_KEYS + teamLabel()에서만 오고 화면에 다시 적지 않았다. ?error=는 invalid·unavailable·weak 셋만 문구로 바꾸고(unavailable만 앰버 — 운영 상태다) ?sent=1이면 폼 대신 안내를 띄운다. login-form에 「계정이 없으신가요? 회원가입」, signup-form에 「이미 계정이 있으신가요? 로그인」을 서로 걸었고 app-sidebar의 HIDDEN_ON에 '/signup'을 더했다. **AC grep 하나가 이 세션에서 문서를 잡았다** — 라우트 주석에 `auth.admin.createUser`를 적었더니 `grep -rn 'auth.admin'`이 걸려서 「관리자 쪽(createUser)」으로 고쳤다 (T9 감사의 guard:env 사례와 같은 자리 — 코드에서 막은 것을 주석에서 흘리면 같은 사고다). 게이트: lint·build 통과, npm test **1756건 전부 통과**(98파일, +44). 첫 회차에 supabase 계약 1건이 PGRST303으로 실패했으나 T8·T9 감사가 기록한 라이브 DB 병렬 접속 간헐 실패이고 단독 재실행 57건·전체 재실행 1756건 모두 통과했다."
    },
    {
      "step": 4,
      "name": "pending-screen",
      "commit": "feat: 승인 대기·거절 화면과 재요청 구현",
      "status": "completed",
      "started_at": "2026-08-27T21:15:09+0900",
      "completed_at": "2026-08-27T21:24:00+0900",
      "summary": "만든 파일 넷 — src/lib/api/same-origin.ts(+test 16건) · src/app/api/auth/rejoin/route.ts(+test 12건) · src/components/auth/pending-notice.tsx · src/app/pending/page.test.ts(8건). page.tsx는 자리표시자에서 세 갈래 화면으로 교체. **isSameOrigin(origin: string | null, host: string | null, proto: string): boolean** — 규칙은 둘뿐이다: **Origin이 없으면 통과, 있는데 다르면 거부.** 없으면 통과시키는 근거는 T8 완료 기준 2가 「curl로 직접 검증한다」를 명시하고 curl은 Origin을 붙이지 않는다는 것 — 「없으면 거부」로 조이면 방어 하나를 얻고 방어를 확인할 방법을 잃는다. 그러고도 CSRF는 막힌다(브라우저는 cross-site POST에 언제나 Origin을 붙이고 폼·fetch 어디에도 그것을 지울 수단이 없다). 빈 문자열·문자열 'null'(샌드박스 iframe)은 「없음」이 아니라 거부다. Host를 모르면 대조할 것이 없어 거부. proto는 'https,http' 겹쳐 쓴 값의 첫 토큰만 보고 'http:'처럼 콜론이 붙은 값(URL.protocol)도 받는다. **x-forwarded-host를 보지 않는다** — 프록시가 겹쳐 쓰지 않는 배치에서는 요청자가 정할 수 있는 값이라 대조 기준이 되지 못한다. 같은 파일의 requestIsSameOrigin(request)가 헤더 이름을 아는 유일한 자리다(x-forwarded-proto ?? new URL(request.url).protocol, origin, host) — 라우트가 직접 꺼내면 다음 라우트가 x-forwarded-proto를 빠뜨리고 그 실패는 프록시 뒤에서만 나타난다. step 9가 전 라우트를 훑을 때 이 함수 하나를 부르면 된다(이 step에서는 rejoin에만 걸었다 — login·logout·signup 미변경, git status로 확인). rejoin 라우트: runtime='nodejs', 갈래 둘(성공 303 /pending / 그 밖 303 /pending?error=invalid — 실패 사유를 갈라도 사용자가 할 일이 「팀을 다시 고르기」 하나라 나누지 않았다). 순서는 Origin 검사 → 폼 읽기 → teamIdSchema → 세션 클라이언트 → rpc. **rpc('request_join', { team })** 인자가 팀 하나뿐이고 대상은 언제나 auth.uid()다(테스트가 toEqual로 못박는다). 사용자 JWT(createSessionClient, anon 키)로 나가고 service_role은 이 경로에 닿지 않는다(AC grep 통과). 회전된 세션 쿠키를 버퍼에 모아 응답에 싣는다(잃으면 조용히 로그아웃). teamIdSchema를 signup-schema.ts에서 export해 가입과 재요청이 같은 팀 목록을 본다(스키마 복사 금지). /pending 화면 갈래 다섯: ok → redirect('/') · anonymous → redirect('/login?next=%2Fpending') · pending → PendingNotice · rejected → RejectedNotice(재요청 폼) · no_profile → MissingProfileNotice. **재요청 폼은 rejected에만 있다** — request_join이 통과하는 유일한 상태라 대기 중에 폼을 주면 눌러 보게 되고 그 실패는 사용자에게 고장으로 보인다. 셋 다 로그아웃 폼(POST /api/auth/logout)이 있다 — 막다른 길이라 나갈 문이 없으면 계정이 갇힌다. 팀 이름은 teamLabel()에서만 오고 화면이 다시 적지 않는다(teamId가 null이면 이름 없이 그린다 — 지어내지 않는다). PageShell을 쓰지 않는다(/login·/signup과 같은 판단). 반려 사유는 보여 주지 않는다 — DB에 사유 칸이 없다. 게이트: lint·build 통과, npm test 1792건 전부 통과(101파일, +36)."
    },
    {
      "step": 5,
      "name": "join-requests-api",
      "commit": "feat: 팀 합류 요청 조회·승인·거절 API 구현",
      "status": "pending"
    },
    {
      "step": 6,
      "name": "team-requests-tab",
      "commit": "feat: 팀원 요청 탭 화면 구현",
      "status": "pending"
    },
    {
      "step": 7,
      "name": "member-tree",
      "commit": "feat: 멤버 트리 도메인 함수 구현",
      "status": "pending"
    },
    {
      "step": 8,
      "name": "members-tab",
      "commit": "feat: 어드민 멤버 탭과 리더 승격 구현",
      "status": "pending"
    },
    {
      "step": 9,
      "name": "security-audit",
      "commit": "test: 권한 상승·열거 방어 검증 테스트 추가",
      "status": "pending"
    },
    {
      "step": 10,
      "name": "docs-update",
      "commit": "docs: 회원가입·승인 흐름을 설계 문서에 반영",
      "status": "pending"
    }
  ],
  "created_at": "2026-08-27T20:18:18+0900"
}