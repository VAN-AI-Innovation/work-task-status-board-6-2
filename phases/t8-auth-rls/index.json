{
  "project": "work-task-status-board",
  "phase": "t8-auth-rls",
  "steps": [
    {
      "step": 0,
      "name": "auth-decisions",
      "commit": "docs: 인증·RLS 경계 결정과 권한 타입 확정",
      "status": "completed",
      "started_at": "2026-08-27T02:46:44+0900",
      "summary": "T8 경계 결정 6건(세션=@supabase/ssr 쿠키+src/proxy.ts / 조회는 사용자 JWT·service_role은 업로드 확정·시드만 / security definer 함수 셋 / unknown_owner는 member 범위 제외 / 데모 모드는 인증 면제·세션이 ?as=를 이긴다 / UNAUTHENTICATED 401 추가·PATCH 필드는 status·progress 둘)을 PLAN 「T8 착수 시 확정」·ADR-024~026·ARCHITECTURE 「권한 (T8)」에 못박고 TICKETS 산출물을 실제 구성으로 고쳤다. 타입은 src/types/auth.ts(Viewer·TaskPatch)뿐이며 ViewerRole·TeamKey는 재선언하지 않고 import한다.",
      "completed_at": "2026-08-27T02:57:42+0900"
    },
    {
      "step": 1,
      "name": "task-permission",
      "commit": "feat: 역할별 열람 범위와 수정 권한 판정 함수 추가",
      "status": "completed",
      "started_at": "2026-08-27T02:58:28+0900",
      "summary": "src/lib/domain/viewer-scope.ts + 테스트 28건 신설 — taskInScope·scopeTasks·goalMetricInScope·scopeGoalMetrics 넷. 갈래는 셋뿐(admin 전부 / lead 같은 팀 / member 담당자 본인, 단 목표 지표는 member도 팀으로 본다 — 담당자 축이 없다). null 가드가 판정보다 먼저 서서 unknown_owner(ownerMemberId=null)와 팀 없는 lead가 새지 않는다. 시계·환경변수·저장소를 보지 않고 Viewer|null도 받지 않는다. 변이 5종(member/lead/goal의 null 가드 제거, scopeTasks 정렬, member 목표 지표를 담당자로 판정)을 넣어 전부 잡히는 것을 확인했고 보강할 것은 없었다 — Task.teamId·GoalMetric.teamId가 non-nullable이라 null 가드는 저장소가 null을 흘리는 런타임을 캐스팅으로 재는 테스트가 따로 있다. 게이트 1345건 전부 통과(supabase 계약 포함) — 이전 시도의 계약 5건 실패는 원격 DB의 일시적 경합(task_events FK 23503)이었고 코드와 무관했다.",
      "completed_at": "2026-08-27T03:07:42+0900"
    },
    {
      "step": 2,
      "name": "repo-contract-ext",
      "commit": "feat: 구성원 조회와 태스크 단건 수정을 저장소 계약에 추가",
      "status": "completed",
      "started_at": "2026-08-27T03:08:21+0900",
      "summary": "TaskRepository에 updateTask(준 필드만 갱신·없으면 null·updatedAt 주입)와 listMembers 둘을 더하고 memory·supabase 두 구현이 같이 통과하게 했다(계약 20→22건, 두 스위트 모두 22). MemberRecord는 types/auth.ts에 둔다(업무가 아니라 신원). supabase는 toTaskRow 대신 부분 갱신 전용 toTaskPatchRow를 써서 주지 않은 컬럼이 null로 덮이지 않고, getTask와 같은 uuid 모양 검사를 앞에 둔다. updateTask는 lastProgressAt·task_events를 건드리지 않는다(사람의 수정은 업로드가 아니다). toReadOnly가 updateTask를 STORAGE_READONLY로 막고 listMembers는 읽기라 통과시킨다. listMembers는 계약이 아니라 두 구현의 각자 테스트가 잰다 — 구성원을 만드는 쓰기 메서드가 제품 코드에 없어서다. 변이 3종(memory가 undefined도 대입 / supabase가 toTaskRow / scope에서 updateTask 위임 제거)을 넣어 전부 잡히는 것을 확인했다. 게이트 1360건 전부 통과(supabase 계약 라이브, SKIP_LIVE_DB 없이). 원격 members는 step 전후 모두 0행이고 contract:: 잔여 0. ⚠ 남은 갈래 하나: supabase는 updateTask가 tasks.updated_at을 쓰고 getLastSyncedAt이 max(updated_at)이라 PATCH가 「마지막 반영」을 앞당기는데 memory는 그렇지 않다. 계약이 재지 않는 자리이고 「PATCH가 반영인가」는 제품 결정이라 step 9에 넘겼다(양쪽 코드에 주석으로 남겼다).",
      "completed_at": "2026-08-27T03:20:17+0900"
    },
    {
      "step": 3,
      "name": "owner-link",
      "commit": "feat: 시트 담당자 이름을 구성원 행에 잇는 해석 계층 추가",
      "status": "completed",
      "started_at": "2026-08-27T03:21:02+0900",
      "summary": "src/lib/upload/owner-link.ts + 테스트 17건 신설 — buildOwnerIndex(팀+정규화 이름 → 구성원 id)와 linkOwners(tasks, members) → {tasks, linked, unresolved} 둘. 규율은 「최대한 붙인다」가 아니라 「확실할 때만 붙인다」다: 같은 팀 안에서만 보고(다른 팀 동명이인은 안 붙는다), 비교 키는 trim → 연속 공백 하나 → NFC(소문자화 없음 — 한글에 무의미하고 영문은 서로 다른 사람일 수 있다), 정규화 후 한 팀에 같은 키가 둘이면 그 키만 통째로 버린다(먼저 온 사람이 이기지 않는다), 이름 없는 행은 unresolved로도 세지 않고, 이미 붙은 ownerMemberId는 덮어쓰지 않는다. 정규화 함수는 이 파일에 뒀다 — cell-normalizer의 toText는 앞뒤 공백만 떼고 row-mapper의 slug는 소문자화·하이픈화라 사람 이름에 쓸 수 없다. 순수 함수이고 던지지 않으며 반환값에 이름을 담지 않는다(건수만). upload-commit의 applyPayload가 upsertTasks 직전에 한 번 부르고, listMembers가 던지면 삼키지 않아 바깥 try가 STORAGE_UNAVAILABLE로 접는다(연결만 조용히 건너뛰면 그 업로드는 영구히 unknown_owner다). UploadSummary에 필드를 더하지 않았다. 변이 4종(팀 비교 제거 / 충돌에서 먼저 온 사람 승 / 기존 id 덮어쓰기 / linkOwners 호출 제거)을 넣어 전부 잡히는 것을 확인했다. 게이트 lint·build·test 1379건 전부 통과 (supabase 계약 라이브, SKIP_LIVE_DB 없이). 앞선 시도가 게이트에서 멈춘 것은 코드가 아니라 next build가 동시에 두 번 돌아 서로 잠긴 것이었고, 순차 재실행으로 사라졌다. AC grep 3종 통과: 제품 코드의 listMembers는 upload-commit.ts 한 줄, owner-link.ts에 시계 0줄, viewer-scope.ts에 ownerNameRaw 0줄(권한이 이름으로 서지 않는다).",
      "completed_at": "2026-08-27T03:31:38+0900"
    },
    {
      "step": 4,
      "name": "auth-rls-migration",
      "commit": "feat: profiles 테이블과 RLS 정책 마이그레이션 추가",
      "status": "completed",
      "started_at": "2026-08-27T03:32:19+0900",
      "completed_at": "2026-08-27T03:41:41+0900",
      "summary": "supabase/migrations/0003_auth_rls.sql 신설 + 원격(ebeylvqmcungiitspaib)에 마이그레이션 t8_auth_rls로 적용. profiles 1테이블 + security definer 함수 3개 + 정책 11개 + 권한 조정이 전부이고 src/ 코드는 한 줄도 안 고쳤다. 정책은 viewer-scope.ts의 표를 글자 그대로 옮긴 것이다 — tasks는 admin 전부 / lead my_team() / member my_member_id()이고 null 가드가 판정보다 먼저 서서 unknown_owner가 member에게 새지 않는다. tasks update는 using=with check로 같은 범위이고, task_stages는 부모 tasks를 select해 tasks 정책을 다시 태운다(규칙을 두 벌로 만들지 않는다). goal_metrics·team_period_goals만 lead·member를 함께 팀으로 보는데, 목표 지표에 담당자 축이 없어서다. MCP 조회 실측: 함수 3행 전부 prosecdef=true·proconfig=[search_path=\"\"]·provolatile=s (완료 기준 4) / pg_policies 11건 전부 roles={authenticated}이고 uploads·task_events·doc_extractions는 0건 / profiles relrowsecurity=true / tasks 행 수 1 → 1 (step 전후 동일, 아무것도 지우지 않았다). 정책에 from profiles 직접 select 0줄(완료 기준 3), 파일에 drop table·truncate·delete from 0줄. 재적용 검증: 같은 SQL을 execute_sql로 한 번 더 돌려 에러 없이 policies 11·funcs 3·tasks 1로 동일. GRANT는 spec의 revoke insert,update,delete보다 한 단계 좁혔다 — Supabase가 authenticated에 기본으로 주는 TRUNCATE가 RLS를 통째로 우회해 전 행을 지우기에, revoke all로 바닥부터 쌓고 select만 되돌려줬다. 결과 실측: anon의 public 테이블 권한 0건, authenticated는 정책 붙은 10개 테이블에 SELECT만, tasks의 컬럼 UPDATE는 progress,status,updated_at 정확히 셋, service_role 91건 무변경. advisors(security): rls_enabled_no_policy가 12건 → 3건(uploads·task_events·doc_extractions — 의도한 서버 전용 셋이라 두고 간다). 새로 뜬 WARN 6건은 anon/authenticated_security_definer_function_executable — my_role·my_team·my_member_id가 /rest/v1/rpc로 호출 가능하다는 지적이고, step 지시가 grant execute를 적지 말라며 이미 판단한 자리다: 셋 다 호출자 자신의 행만 돌려주고 anon에게는 auth.uid()가 null이라 전부 null이다. public의 execute를 회수하면 정책 평가 자체가 permission denied로 깨진다. 문서 정합: 0001_init.sql 맨 아래에 「→ 0003_auth_rls.sql이 붙였다」 한 줄 추가, ARCHITECTURE.md 「정책 표」를 실제 적용 결과로 정정했다 — 초안이 uploads·doc_extractions를 admin·lead에 열고 task_events를 부모 tasks에 딸리게 했으나 셋 다 정책 없이 닫았고(S6), 컬럼 제한이 API 단독이 아니라 GRANT와 함께 진다는 것과 truncate 회수 근거를 함께 적었다. 게이트 lint·build·test 1379건 전부 통과(supabase 계약 라이브, SKIP_LIVE_DB 없이). ⚠ 첫 실행에서 upload-record-store 계약 1건이 PGRST303으로 실패했으나 단독 재실행 18건 통과·전체 재실행 1379건 통과 — 라이브 DB 파일이 병렬로 붙을 때 나는 일시적 오류이고 이 step의 권한 변경과 무관하다(service_role 경로이고 그 권한은 손대지 않았다)."
    },
    {
      "step": 5,
      "name": "auth-accounts",
      "commit": "chore: 역할 계정 시드와 RLS 실효 검증 스크립트 추가",
      "status": "completed",
      "started_at": "2026-08-27T03:42:24+0900",
      "summary": "scripts/db/seed-auth.ts(npm run seed:auth)와 scripts/smoke/rls-check.mjs 둘을 신설하고 .env.example에 T8_SEED_PASSWORD·T8_SEED_EMAIL_DOMAIN을 빈 값으로 더했다. src/ 제품 코드는 한 줄도 고치지 않았다. seed-auth는 멱등하다 — 1회차 members 6 생성/계정 3/profiles 3/구성원 연결 2/시드 확정 9건, 2회차 전부 '이미 있음'이고 에러 없이 끝난다(생성 0·확정 0). 이름을 하드코딩하지 않고 buildSeedPayload()의 (팀, 담당자) 조합을 세어 members를 만들고, member 계정은 edit 팀에서 태스크가 가장 많은 이름에(동률은 이름 오름차순으로 결정적) lead는 그 다음 이름에 잇는다. 태스크는 uploads.create → commitUpload라는 /api/uploads/seed와 같은 제품 경로로 넣고, 잔여 백필은 owner-link.ts의 buildOwnerIndex를 import해 쓴다(규칙이 두 벌이 아니다). 비밀번호는 없으면 32바이트 난수를 .env.local에 덧붙이고 값은 찍지 않는다 — 표준출력·커밋·문서 어디에도 없고 git status에 .env.local이 없다. rls-check.mjs는 anon 키로 세 계정에 실제 로그인해 잰다(anon===service_role이면 스스로 종료 코드 2로 죽는다). 10개 항목 전부 통과: 1 로그아웃 0행 / 2 admin 10=전체 10 / 3 lead 6=팀 6 <전체 10 / 4 member 2=담당 2 <lead 6 / 5 세 건수 10·6·2로 서로 다름 / 6 남의 태스크 update 갱신 0행 / 7 자기 태스크 update 1행(원래 값으로 되돌림) / 8 title update 거부 42501(grant update 컬럼 제한) / 9 member의 uploads 0행 / 10 admin의 profiles 1행. 기대값 3·4는 하드코딩하지 않고 service_role로 그때그때 센다. 변이 확인 둘 다 성립: anon 자리에 service_role을 넣으면 가드가 종료 코드 2로 막고, tasks_select_scope를 using(true)로 바꾸면 정확히 3·4·5만 FAIL(lead 10·member 10)한다 — 확인 후 원래 정책으로 되돌려 pg_policies의 qual이 0003_auth_rls.sql과 글자 그대로 같은 것을 확인하고 재실행 10건 통과. MCP 실측: profiles 3 / members 6 / auth_user_id 붙은 members 2 / tasks 10 / owner_member_id 붙은 tasks 9 / 실업무 행 1건 그대로 · goal_metrics 3 · 탐침 잔여 0. 문서는 scripts/smoke/README.md 표+실행법, scripts/smoke/RESULT.md 「T8 RLS 실효」 절(무엇을 증명하고 무엇을 증명하지 않는지·변이 표 포함), README.md 명령어 표에 각각 반영. 게이트 lint·build·test 1379건 전부 통과(supabase 계약 라이브, SKIP_LIVE_DB 없이).",
      "completed_at": "2026-08-27T03:54:11+0900"
    },
    {
      "step": 6,
      "name": "auth-session",
      "commit": "feat: 쿠키 세션을 열람자로 해석하는 인증 계층 추가",
      "status": "completed",
      "started_at": "2026-08-27T03:54:59+0900",
      "summary": "src/lib/auth/ 둘 신설 — session-client.ts(anon 키 + 쿠키 세션 클라이언트, 테스트 9건)와 viewer-session.ts(쿠키 → 검증된 사용자 → Viewer, 테스트 32건). @supabase/ssr@^0.12.5 하나만 설치했고(lock에 늘어난 것은 그 패키지와 cookie 뿐) src/ 제품 코드의 나머지는 한 줄도 안 고쳤다. 규율 둘을 여기서 못박았다: (1) 세션을 쿠키에서 디코드만 하는 API를 부르지 않고 auth.getUser()로 Auth 서버에 물어본다 — 가짜 클라이언트가 그 이름에 덫을 놓아 불리면 실패시키고(미인증·프로필 없음·정상 세 갈래 모두), 구현 파일에 그 이름이 없다는 것은 grep AC가 지킨다. (2) 역할은 JWT가 아니라 profiles에서 온다 — RLS의 my_role()과 같은 표를 봐야 화면과 데이터가 어긋나지 않는다. createSessionClient는 자격증명이 없거나 URL 형식이 깨져 있으면 던지지 않고 null이고(데모 모드가 죽으면 안 된다), service_role을 쓰지 않으며(완료 기준 5의 조건), setAll이 던지면 삼킨다(서버 컴포넌트에서는 쿠키를 쓸 수 없고 갱신은 step 10의 proxy가 한다). resolveSession은 client를 인자로 받고 어떤 실패도 anonymous·no_profile로 접는다 — 알 수 없는 role(owner·''·null)은 흘려보내지 않고 no_profile이다(흘려보내면 viewer-scope의 switch가 어느 갈래에도 안 걸려 「아무것도 안 보임」이 되고 원인이 화면에서 드러나지 않는다), team_id가 TeamKey 셋 밖이면 teamId만 null이고 역할은 살며, members 0행은 memberId=null(결정 D의 unknown_owner)이고 ok이긴 하다. members 선택은 order('id').limit(1)로 0003_auth_rls.sql의 my_member_id()와 같은 결정 규칙이고 테스트가 그 호출을 직접 잰다 — 다르면 화면과 DB가 서로 다른 구성원을 「나」로 본다. 읽는 테이블은 profiles·members 둘뿐이고(tasks 0회) 프로필이 없으면 members를 읽으러 가지 않는다. 얇은 호출부(getViewer 같은 전역 헬퍼)·proxy.ts·createBrowserClient는 만들지 않았다 — 쿠키 어댑터가 호출부마다 다르고 하나로 묶으면 그 차이가 감춰진다(step 7·10의 일). 변이 3종을 넣어 확인했다: getUser→세션 디코드 API로 바꾸면 덫 단언 3건이 잡고, 알 수 없는 role을 통과시키면 해당 케이스 4건이 잡는다. ⚠ 세 번째(anonKey 대신 service_role)는 이 셸에서 우연히 테스트 2건도 실패했지만 그것은 .env.local이 그 키를 갖고 있어서일 뿐이고, 키 없는 환경에서는 테스트로 안 잡힌다 — 이 규칙을 지키는 것은 grep AC와 리뷰다. AC grep 4종 통과: SERVICE_ROLE 0줄 / 구현의 세션 디코드 API 이름 0줄 / 환경 전역 읽기 0줄(주석과 단언에 남아 있던 literal도 지웠다 — 규칙은 signature와 grep이 진다) / 테스트의 덫 8줄. 게이트 lint·build·test 1420건 전부 통과(supabase 계약 라이브, SKIP_LIVE_DB 없이).",
      "completed_at": "2026-08-27T04:03:03+0900"
    },
    {
      "step": 7,
      "name": "viewer-storage",
      "commit": "feat: 조회를 사용자 JWT로 내보내는 열람자 저장소 추가",
      "status": "completed",
      "started_at": "2026-08-27T04:03:45+0900",
      "summary": "src/lib/store/viewer-storage.ts + 테스트 7건 신설 — resolveViewerContext(base, client) 하나뿐이고 갈래는 둘이다: base.mode==='live' && client!==null이면 repo=createSupabaseTaskStore(client)·session=await resolveSession(client), 그 밖이면 repo=base.repo·session={status:'anonymous'}. 라이브인데 미인증이어도 JWT 저장소를 그대로 쓴다 — RLS가 0행을 돌려주는 것이 정직한 결과이고 여기서 base.repo로 되돌리면 「로그인 안 했는데 전부 보인다」가 된다. demo·fallback에서는 클라이언트가 있어도 세션을 보지 않는다(메모리 저장소에 그 사용자의 행이 없고 데모는 ?as=가 역할을 정하는 자리다) — 테스트가 getUser 호출 수 0으로 잰다. base는 모든 갈래에서 손실 없이 통과한다(uploads·driver·mode·readOnly): 업로드 확정·시드·업로드 이력은 여전히 service_role로 나가야 하고, 확정은 시트 전체를 써서 올린 사람의 범위 밖 행이 반드시 섞인다. 캐시하지 않는다 — store-factory의 Symbol.for는 「프로세스당 하나」라서 있는 것이고 이것은 「요청당 하나」다. 싱글턴에 사용자 토큰을 넣으면 한 사용자의 토큰이 다음 요청의 다른 사용자에게 샌다. process.env를 읽지 않고 권한을 판정하지 않으며(범위는 viewer-scope.ts와 RLS가 진다) service_role 클라이언트가 이 파일에 등장하지 않는다. 테스트는 resolveSession을 mock으로 갈아끼우지 않고 가짜 클라이언트를 손으로 지어 auth.getUser 호출 수를 세고, StorageHandle도 캐스팅 껍데기가 아니라 진짜 memory 구현을 담는다. 변이 3종(demo에서도 세션 사용 → 데모·폴백 2건 / 라이브+미인증에서 base.repo로 되돌림 → 해당 1건 / 모듈 지역 변수 캐시 → 6건)을 넣어 전부 잡히는 것을 확인했다. store-factory.ts·TaskRepository·라우트·페이지는 한 줄도 고치지 않았고 호출부는 step 8이 붙인다 (grep viewer-storage src/app src/components → 0줄). AC 4종 통과: viewer-storage.ts에 process.env·Symbol.for 0줄 / store-factory.ts diff 없음 / 호출부 0줄 / 게이트 lint·build·test 1427건 전부 통과(supabase 계약 라이브, SKIP_LIVE_DB 없이). ⚠ 첫 게이트에서 upload-record-store 계약 1건이 PGRST303으로 실패했으나 단독 재실행 18건·전체 재실행 1427건 통과 — step 4에서 이미 관측된 라이브 DB 병렬 접속의 일시적 오류이고 이 step의 변경과 무관하다(service_role 경로이며 이 파일은 그 키를 쓰지 않는다).",
      "completed_at": "2026-08-27T04:12:23+0900"
    },
    {
      "step": 8,
      "name": "viewer-role-gate",
      "commit": "feat: 세션 역할이 as 쿼리를 이기도록 열람 경계를 바꾼다",
      "status": "completed",
      "started_at": "2026-08-27T04:13:01+0900",
      "summary": "조회 경로 10곳(라우트 6·서버 컴포넌트 4)을 getStorage()에서 currentViewerContext()로 옮기고 세션이 ?as=를 이기게 했다. 새 파일은 src/lib/auth/request-viewer.ts(테스트 3건) 하나뿐이고 Next의 cookies()를 만지는 유일한 자리다 — 요청 스코프 밖에서 던지는 갈래를 삼켜 anonymous로 접는다(화면이 백지가 되면 안 된다). resolveViewerRole은 세션을 3번째 인자로 받고 판정이 셋으로 늘었다: 세션 ok → 그 역할(환경 무관) / 프로덕션+실저장소 → member(S4) / 그 밖 → ?as= 해석(ADR-013). no_profile은 세션으로 치지 않아 프로덕션+live에서 ?as=로 승격되지 않는다. buildReadContext는 StorageHandle 대신 ViewerContext를 받아 조회를 view.repo(JWT)로 하고 meta의 driver·mode·readOnly만 view.base에서 읽으며, 범위 거르기(scopeTasks)를 overdue 거르기 '앞에' 두고 ReadContext.viewer를 실어 보낸다(step 9·11이 같은 판정을 다시 하지 않도록). 라이브에서는 RLS가 이미 걸러 이 거르기가 대개 no-op이지만 두 층을 다 둔다 — 데모·폴백에는 RLS가 없어 이 줄이 유일한 층이다. api-error에 UNAUTHENTICATED(401·'로그인이 필요합니다.')를 더했고 라우트가 명시적으로 낼 코드라 toApiErrorCode는 손대지 않았다. 라이브 실측(개발 서버 + 시드 계정 3종의 실제 쿠키)이 이 step의 핵심 증거다: admin 10건(edit·shoot·marketing) / lead 6건(edit) / member 2건(edit)으로 갈리고 rls-check.mjs의 DB 계층 수치와 정확히 같다(완료 기준 5). member 세션에 ?as=admin·?as=lead를 붙여도 셋 다 tasks=2·meta.role=member다 — 개발 서버에서도 URL이 지지 않는다(완료 기준 6·ADR-026). 업로드 확정·시드·commit·/api/health는 getStorage()(service_role) 그대로다. 변이 4종 확인: (1) 세션 우선 규칙을 맨 아래로 → 4건 실패 (2) base.repo로 조회 → 11건 실패 (3) scopeTasks를 flags 표를 만든 뒤로 → 모수 단언 2건 실패 (4) UNAUTHENTICATED 문구 한 글자 → 문서 대조 1건 실패. ⚠ (3)의 '두 줄만 맞바꾸는' 형태는 잡히지 않고 그게 맞다 — flags 표를 최종 tasks에서 다시 만들기 때문에 의미가 같다. 실제로 갈라지는 것은 '표를 만든 뒤로 미는' 쪽이고 그것을 잰다. ⚠ 남은 갈래 하나(step 10에 넘긴다): 라이브에서 로그인 없이 조회하면 tasks 0건이 아니라 503 STORAGE_UNAVAILABLE이다. 0003_auth_rls.sql이 anon에게서 테이블 권한을 통째로 회수해 (S5, step 4의 의도된 결정) RLS에 닿기 전에 42501 permission denied로 막히기 때문이다. 정보는 새지 않지만 문구가 '잠시 후 다시 시도'라 사실과 다르다 — 라우트에서 401로 막는 것은 이 step의 금지사항이고(데모가 죽는다) DB grant를 되돌리는 것은 step 4의 결정을 뒤집는 것이라 둘 다 하지 않았다. 이 상태를 없애는 것은 proxy다(세션 없으면 저장소에 닿기 전에 /login). ARCHITECTURE.md 「조회와 쓰기는 다른 클라이언트로」 절에 ⚠ 문단으로 남겼다. 문서: ARCHITECTURE.md 「권한 (T8)」과 PLAN.md S4에 '데모 모드에서는 범위가 갈리지 않는다'를 적었다(?as=lead에 붙일 팀도 구성원도 없어서 바뀌는 것은 섹션 배치와 extras 마스킹뿐이고 범위 구분은 로그인했을 때만 일어난다) — 적어 두지 않으면 다음 사람이 데모에서 재 보고 '권한이 안 걸린다'고 결론 낸다. viewer-role.ts 머리말의 '인증은 T8이다/그때까지'는 현재형으로 고쳤다. 게이트 lint·build·test 1446건 전부 통과(supabase 계약 라이브, SKIP_LIVE_DB 없이). AC grep: 조회 경로의 getStorage 0줄 / uploads·health는 그대로 / cookies() 실호출은 request-viewer.ts 한 줄(나머지 히트는 session-client.ts와 같은 파일의 주석 산문이라 손대지 않았다) / UNAUTHENTICATED가 코드와 문서 양쪽에 / '그때까지' 0줄.",
      "completed_at": "2026-08-27T04:26:58+0900"
    },
    {
      "step": 9,
      "name": "patch-api",
      "commit": "feat: 태스크 수정 API에 서버 측 권한 검증 추가",
      "status": "completed",
      "started_at": "2026-08-27T04:27:40+0900",
      "summary": "PATCH /api/tasks/[id] 신설 — T8 완료 기준 2(「member가 타인의 태스크에 PATCH를 보내면 서버가 FORBIDDEN으로 거부한다」)의 실체. 새 파일은 src/lib/api/task-patch-schema.ts(테스트 12건) 하나이고 GET은 한 줄도 안 고쳤다. 스키마는 .strict()에 status(trim 후 1~100자, enum으로 좁히지 않는다 — 시트 값은 설정 탭에서 오고 늘어난다·ADR-009)와 progress(0~100 정수 또는 null) 둘뿐이며, progress:null(값을 지운다)과 키 없음(안 건드린다)을 구분하고 빈 객체 {}는 거부한다(아무것도 안 바꾸는 요청이 200이면 클라이언트 버그가 성공으로 보인다). 라우트의 갈래 순서는 401 UNAUTHENTICATED → 503 STORAGE_READONLY(저장소를 건드리기 전에) → 400 VALIDATION_FAILED(JSON 아닌 본문 포함) → getTask null 403 → taskInScope 403 → updateTask null 403 → 200이고, 판정은 taskInScope·플래그는 deriveTaskFlags·직렬화는 toTaskResponse·meta는 buildReadContext라 라우트에 계산이 0줄이다. now는 라우트가 만들어 넘긴다. **존재 여부를 알려주지 않는다** — 인증된 사용자에게 이 라우트는 TASK_NOT_FOUND를 내지 않고 보이지 않는 행도 없는 행도 403이다(부원이 id를 훑어 전사 업무의 존재·개수를 세는 것을 막는다·S6). GET의 404는 그대로 뒀고 두 코드가 갈리는 이유를 라우트 주석과 ARCHITECTURE.md 「같은 자원, 다른 코드」에 남겼다. 읽기·쓰기 모두 사용자 JWT(view.repo)이고 service_role 핸들(view.base.repo)은 이 파일에서 한 번도 쓰이지 않는다(주석 언급 1줄뿐). 테스트는 route.test.ts에 23건으로 늘었다 — 거부 케이스마다 상태 코드만이 아니라 updateTask 미호출을 함께 재고(403인데 쓰기는 이미 나간 상황이면 상태 코드가 거짓말이다), currentViewerContext는 override가 null이면 진짜 구현으로 넘겨 기존 GET 테스트 경로를 그대로 뒀다. 변이 4종 전부 잡혔다: 401 가드 제거→2건 / taskInScope 제거→1건(「저장소가 남의 건을 돌려줘도」 — 이 줄을 재는 유일한 케이스) / getTask null을 404로→2건 / .strict() 제거→2건. memory-task-store.ts가 step 9에 넘긴 ⚠(「PATCH가 반영인가」)를 여기서 정했다: **PATCH는 「반영」이 아니다** — 화면의 「마지막 반영」은 업로드가 돌아간 시각이고(ADR-001), supabase만 tasks.updated_at이 not null이라 getLastSyncedAt이 앞당겨지는 갈래가 남는 것을 알고 둔다(라우트 주석에 명시). 문서: ARCHITECTURE.md 「에러 처리」에 「같은 자원, 다른 코드」 문단, PLAN.md UC-16 줄에서 「(T8 이후)」를 떼고 실제 동작(둘만 받는다·남의 건은 보이든 안 보이든 403·미인증 401)으로 고쳤다. AC grep 전부 통과: runtime='nodejs' 있음 / getStorage 0줄 / ownerNameRaw 0줄 / FORBIDDEN 정확히 3. 게이트 lint·build·test 1476건 전부 통과(supabase 계약 라이브, SKIP_LIVE_DB 없이). ⚠ 첫 실행에서 supabase 계약 1건이 PGRST303으로 실패했으나 재실행 1476건 통과 — step 4·7에서 이미 관측된 라이브 DB의 일시적 오류이고 이 step은 그 경로(service_role)를 건드리지 않았다. **라이브 curl 검증은 step 12(감사)에서 한다** — 로그인 경로가 아직 없어 이 step에서 세션 쿠키를 만들 방법이 없다. 다만 DB 층의 같은 방어는 step 5의 rls-check.mjs 항목 6(남의 태스크 update 갱신 0행)·7(자기 태스크 update 1행)·8(title update 42501)이 이미 증명했다.",
      "completed_at": "2026-08-27T04:35:47+0900"
    },
    {
      "step": 10,
      "name": "login-page",
      "commit": "feat: 로그인 화면과 보호 라우트 리다이렉트 추가",
      "status": "pending"
    },
    {
      "step": 11,
      "name": "role-gating-ui",
      "commit": "feat: 로그인 세션에 따라 화면 범위와 상단 바를 가른다",
      "status": "pending"
    },
    {
      "step": 12,
      "name": "t8-audit",
      "commit": "docs: T8 완료 기준 전수 검증과 문서 갱신",
      "status": "pending"
    }
  ],
  "created_at": "2026-08-27T02:46:44+0900"
}